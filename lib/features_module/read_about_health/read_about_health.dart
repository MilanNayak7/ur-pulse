import 'package:flutter/material.dart';

class AboutHealth extends StatelessWidget {
  const AboutHealth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("AboutHealth")),
      ),
    );
  }
}
